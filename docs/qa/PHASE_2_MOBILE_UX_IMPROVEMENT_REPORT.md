# KindWorks Phase 2 — Mobile UX Improvement Report

## Working baseline

- Branch: `phase-2-mobile-ux-improvement`
- Starting commit: `ae262cd87a443c64f40ce9ed043303c1456804b5`
- Starting branch: `main` (matched `origin/main`)
- Phase 1 contract: `docs/qa/PHASE_1_MIGRATION_PARITY_AUDIT.md`
- Baseline automated tests: 450 passed, 0 failed
- Current automated tests: 485 passed, 0 failed after the Morning Mug batch
- Baseline build: existing production `dist`, Phaser 4.2.1
- Baseline input: mouse and keyboard through the local browser preview
- Available visual reference: `KindWorks_Visual_Style_Bible_v3.0.pdf`
- Required but unavailable reference: Visual Style Bible v4 and `KW-REF-HOUSE-A-V4`

The v3 reference is used provisionally for established KindWorks tokens only: warm-cloud/cream panels, deep-ink text, sunflower primary actions, willow-green success, terracotta warnings, rounded 16–24px corners, soft shadows, minimum 44px touch targets, mobile-readable silhouettes and one focal effect at a time. No v4-specific art or visual decision will be invented.

## Required viewport matrix

| Device class | Viewport | Baseline captured | Current observation |
| ------------ | -------: | ----------------- | ------------------- |
| Small phone | 568×320 | Yes | Town HUD uses four essential top items; objective and contextual action do not overlap; all eight menu destinations fit without scrolling |
| Phone | 667×375 | Yes | Town HUD, compact objective and 44px menu target pass without crop or overflow |
| Phone | 736×414 | Yes | 721–900px compact-objective breakpoint prevents prompt overlap; 44px menu target passes |
| Wider phone | 812×375 | Yes | Compact objective and action-led prompt preserve the playable map; no document overflow |
| Modern phone | 844×390 | Yes | Town hierarchy, prompt separation and menu target pass with exact viewport dimensions |
| Tablet | 1024×768 | Yes | Full first-job checklist fits without touching the contextual action |
| Large tablet | 1180×820 | Yes | Full town layout passes with no crop or overflow |
| Original reference | 1280×720 | Yes | Full town layout passes with no crop or overflow |
| Desktop QA | 1366×768 | Yes | Full town layout passes with no crop or overflow |
| Portrait safety | 390×844 | Yes | Town pauses behind one rotate icon and resumes at the exact map position |

Baseline evidence is stored outside the repository under `phase2-evidence/baseline/` in the Codex task workspace.

## Screen improvement register

| Screen | Original problem | Change made | Viewports tested | Gameplay regression result | Before evidence | After evidence | Commit |
| ------ | ---------------- | ----------- | ---------------- | -------------------------- | --------------- | -------------- | ------ |
| Wave 0 baseline | No protected Phase 2 branch, tracking report or full viewport baseline | Created branch, captured all required baseline sizes and established the functional contract | All required sizes plus 390×844 portrait | 450/450 tests passed before changes | `phase2-evidence/baseline/` | N/A | `593b456` |
| Global landscape shell | Portrait displayed active, overlapping town gameplay and did not pause its systems | Added one global orientation controller, a single-sentence safe rotate state, exact game-loop freeze/wake, service pause reasons and River portrait exemption | 568×320, 667×375, 736×414, 812×375, 844×390, 1024×768, 1180×820, 1280×720, 1366×768, 390×844 | Production build and performance budget pass; 454 full tests pass; movement, wallet open/close, rotate/resume and saved town state pass | `phase2-evidence/baseline/town-390x844-before.png` | `phase2-evidence/wave1/town-390x844-after.png` plus full Wave 1 matrix | `1469a93` |
| Shared control states | Controls used unrelated pressed, disabled, selected and focus treatments; dynamically rendered controls had no shared state contract | Added reusable tokens and one delegated controller for normal, pressed, disabled and selected states across fixed and dynamic buttons | All required landscape sizes plus 390×844 portrait; wallet inspected at 844×390 | 206/206 live controls enhanced on the saved test game; wallet tab switch and close pass; no overflow; no runtime warning/error logs; production build and performance budget pass; 456/456 tests pass | `phase2-evidence/wave2/wallet-844x390-before.png` | `phase2-evidence/wave2/wallet-844x390-after.png` | `1d8cb7e` |
| Shared loading and error feedback | Lazy activity imports changed an invisible attribute and failures were console-only, leaving players without a clear transition or recovery message | Added one shared `Loading…` state and one dismissible “That area couldn’t open. Try again.” notification, wired into every lazy scene transition | 568×320, 844×390, 1024×768, 1280×720 and 390×844 portrait in Waste Collection | Entered through the normal town action, loading cleared, portrait paused, safe exit returned to the same position, Sprite AI coverage stayed complete, runtime logs clean, build/performance pass and 458/458 tests pass | No visible loading/error component existed | `phase2-evidence/wave2/waste-844x390-loading-cleared.png` | `cc89540` |
| Town HUD and secondary navigation | Seventeen visible buttons competed with the map; eight secondary destinations occupied the top HUD; the first-job card and nearby-action card overlapped at phone breakpoints; Waste Collection inherited the town objective; compact-menu labels and Save fell outside the smallest panel | Kept day, weather, coins and one Menu entry; moved all eight secondary destinations into a pausing, focus-contained menu; shortened labels; compacted the current objective and contextual detail at phone widths; hid town-only objective/menu outside Town; added width- and height-adaptive layouts | All nine required landscape sizes plus 390×844 portrait; menu inspected at 568×320 and 844×390 | Menu-to-Inventory, Escape, complete Tab focus cycle, touch/keyboard movement, portrait exact-state resume, normal Town-to-Waste entry and safe return all pass; location remains exact, wallet remains 100, runtime logs clean, build/performance pass and 460/460 tests pass | `phase2-evidence/baseline/town-568x320-before.png`; `phase2-evidence/baseline/town-844x390-before.png` | `phase2-evidence/wave3/` full matrix, including `town-568x320-after.png` and `town-menu-568x320-after.png` | `3c7034e` |
| Lawn Care | At 568×320 every mower direction and the status message were below the viewport; the scrollable fixed-width HUD distorted the square board; phone and tablet controls were below the 44px target; instructions repeated mechanics and `Save & exit` did not describe its two-step cancellation flow | Added a non-scrolling three-row landscape shell, square aspect-preserving board, unused-width D-pad, compact contextual side rail, always-visible feedback, 44px controls, short action-led messages and an accurate `Exit` label | All nine required landscape sizes plus 390×844 portrait; success and failure at 568×320 | Live swipe changed 7%/0 moves to 29%/1 and Undo restored 7%/0; safe-route completion reached 100% in 9 moves and paid exactly +100; 29%/11-move failure paid +0; Replay, confirmed Exit, exact Town return, rotation, active-attempt reload and clean runtime logs pass; build/performance pass and 462/462 tests pass | `phase2-evidence/wave4-lawn/lawn-568x320-before.png`; `phase2-evidence/wave4-lawn/lawn-844x390-before.png` | `phase2-evidence/wave4-lawn/` full matrix, including `lawn-568x320-after.png` and `lawn-568x320-success-after.png` | `54150a2` |
| River Clear-Out | River was allowed in both orientations despite its tall 10×16 Tetris board; the 390×844 board used only 252×403 pixels; at 320px wide the original side rail clipped the Hint control; the level picker and result used almost full-height empty panels; Exit was only 37px wide and instructions repeated the completion rule | Made River the explicit portrait-only exception, added the one-sentence upright-device pause state, rebuilt the portrait shell around an aspect-correct board, added a 320px control-rail breakpoint, 44px controls, compact level/reward cards and shorter contextual copy | All nine required landscape sizes as protected rotate states; 320×568, 375×667, 390×844, 414×736, 768×1024 and 820×1180 portrait gameplay | Real movement/rotation/drop/undo and Hint pass; six unplanned drops produced the expected 0%, +0 failure; Replay reset exactly; certified completion produced 100%, three rows, three stars and +100; wallet persisted at 310 after reload; two-step Exit and exact Town return pass; production build/performance pass and 464/464 tests pass | `phase2-evidence/wave4-river/river-390x844-before.png`; `phase2-evidence/wave4-river/river-390x844-picker-before.png` | `phase2-evidence/wave4-river/river-390x844-production-after.png`; `phase2-evidence/wave4-river/river-320x568-production-after.png`; `phase2-evidence/wave4-river/river-390x844-success-after.png`; `phase2-evidence/wave4-river/river-844x390-rotate-after.png` | `cdc0ab4` |
| Waste Collection | The first-job Exit and six item choices were only 32–37px high and disabled Finish occupied the HUD; in the campaign at every phone size through 844×390, the board was visible but the five-slot tray, actions and status sat below the viewport in an internally scrolling panel; cards measured as little as 24px high; picker and feedback repeated the rules | Kept the board and live exposed-card engine, moved the five-slot tray and contextual actions into a compact side rail on short landscape screens, fixed the status to the safe bottom row, raised every card/control to 44px, hid Restart until a move and Hint on failure, removed collected first-job choices, revealed Finish only when ready, and shortened labels/messages | All nine required landscape sizes plus 390×844 portrait rotation; first job and campaign at 568×320; production campaign at 568×320 | Six real first-job pickups paid the protected +100; five distinct campaign cards produced the exact tray-full failure; Restart restored 30 cards and 0/5 tray; certified Level 2 clear paid +100; replay paid +0; rotation retained 29 cards and 1/5 tray; two-step Exit, Town return and 510-coin reload pass; build/performance pass and 466/466 tests pass | `phase2-evidence/wave4-waste/waste-job-568x320-before.png`; `phase2-evidence/wave4-waste/waste-campaign-568x320-before.png`; full baseline matrix | `phase2-evidence/wave4-waste/waste-job-568x320-after.png`; `phase2-evidence/wave4-waste/waste-campaign-568x320-production-after.png`; `phase2-evidence/wave4-waste/waste-campaign-result-568x320-after.png`; full after matrix | `619d24c` |
| Playground Power Wash | The board canvas had no `powerwash-board` class, so its interactive surface remained at the intrinsic 960×480 size while the visible frame clipped it; on phones the board, tool rail and status extended below the viewport; active controls were 31–36px; Supplies repeated the top resource counters; Restart occupied space before it was useful; and no washer wand was visible | Bound the canvas to its aspect-correct frame, added a labelled visible wand, rebuilt short landscape play as a fixed header/play/status shell with a 148px two-by-two tool rail, hid duplicate supplies and contextual Restart until needed, enforced 44px controls and shortened labels and feedback | All nine required landscape sizes plus 390×844 portrait; picker, active play, failure, result and production build at 568×320 | All four tools and continuous touch washing pass; Restart restores protected defaults; water exhaustion pays +0; Level 1 town job pays exact +101; Level 2 first clear pays +100 and replay +0; 97% tolerance, Level 750 reward cap and save integration pass in protected tests; rotation, two-step Exit, exact Town return and 711-coin reload pass; build/performance pass and 468/468 tests pass | `phase2-evidence/wave4-powerwash/` full before matrix, including `powerwash-568x320-before.png` | `phase2-evidence/wave4-powerwash/` full after matrix, including `powerwash-568x320-production-after.png`, picker and result evidence | `e8d9cec` |
| Beach Cleanup | The fixed 320px board minimum, side stack and separate control row overflowed every required landscape viewport: at 568×320 the board ended at y=470, walking controls at y=504 and feedback at y=533; even 1280×720 hid controls and status below the HUD; persistent optional challenges competed with movement; controls measured 30–39px | Rebuilt the activity as a fixed landscape header/play/status shell, kept the responsive board in the main column, moved a 44px D-pad and contextual actions into a compact 148px rail on phones, placed three optional challenges behind one Bonus disclosure, hid Undo/Restart until useful, centred picker/result cards, and added short action, blocked-step, find, undo and challenge feedback | All nine required landscape sizes plus 390×844 portrait; Level 1 town job, campaign Level 2, Level 750 and production build at 568×320 | Real swipe and 59-step town route pass; blocked movement explains the obstacle; Undo and Restart restore exact state; town job paid protected +6; Level 2 first clear paid +100 and replay +0; active rotation/reload and two-step Exit pass; Level 750 15×13/50-item board fits; no terminal failure exists in the validated engine, so its recoverable blocked-input path was tested; build/performance pass and 470/470 tests pass | `phase2-evidence/wave4-beach/` full before matrix, including `beach-568x320-before.png` | `phase2-evidence/wave4-beach/` full after matrix, including production, Level 750, Bonus and result evidence | `abfeec5` |
| House Rescue | At 568×320 the sorting floor and bins ended at y=406.5, QA at y=449 and status at y=481.5; the same essential feedback remained below every phone viewport through 844×390; Exit and QA were 31–39px; picker and stage copy repeated rules; the result exposed two buttons with the same return action; a saved rescue could also appear inside a different dirty cottage | Rebuilt House Rescue as a safe-area fixed header/play/status shell, fitted all nine items beside three 44px bins, kept the complete vacuum board visible, moved development QA into the header, shortened contextual copy, added selected/correct/wrong feedback, merged the duplicate result actions, centred picker/result cards, corrected the global portrait transform and blocked a saved rescue from opening in the wrong cottage with a named redirect | All nine required landscape sizes plus 390×844 portrait; picker, sorting, wrong sort, vacuum, result, saved-rescue guard and production smoke | Real touch sorting preserved +2/−1; manual swipes reached the protected 97% completion threshold and paid the exact +96 one-mistake reward; wallet changed 26,116→26,212 once; return and saved Level 2 resume pass; wrong-cottage entry is disabled and points to Morningbell; rotation retained 8 items/score 2/mistakes 0; production QA is absent; no logs; build/performance pass and 473/473 tests pass | `phase2-evidence/wave4-house-rescue/` full before matrix, including `house-rescue-sort-568x320-before.png` | `phase2-evidence/wave4-house-rescue/` full after matrix, including picker, vacuum, result, guard and portrait evidence | `73d561e` |
| Fishing and Magnet Fishing | At 568×320 the fixed bottom card occupied y=90.5–314 across 556px, covering almost the complete river; Cast/Retrieve were both always present at 40px and Exit was 35.5px; the empty result sentence repeated that nothing had happened; wider sizes kept a large bottom-left panel rather than adapting around the water | Replaced the bottom card with a safe-area responsive side rail, kept roughly three-quarters of the smallest-phone river visible, enforced 44px controls, shortened labels and prompts, showed only Cast or Reel/Pull when useful, hid the empty result row and added a two-step exit while a cast is pending | All nine required landscape sizes plus 390×844 portrait; both modes, active cast, miss, success, exit, return and production shell | Fish miss/recovery and successful timed catch pass; River Minnows entered inventory once with no coin change; pending Magnet exit preserved the used cast and granted no reward; Sealed Coin Tin paid exactly +350 once; rotation retained exact phase/casts/result; all visible controls are at least 44px, no overflow; build/performance pass and 476/476 tests pass | `phase2-evidence/wave4-fishing/` full before matrices, including `fishing-568x320-before.png` and `magnet-568x320-before.png` | `phase2-evidence/wave4-fishing/` full after matrices, including `fishing-568x320-after.png`, `magnet-568x320-after.png` and `fishing-390x844-after.png` | `fdb93d3` |
| Little Bakery | At 568×320 the full-width HUD covered the customer and preparation scene, ended at y=315 and internally scrolled; only four of seven recipe steps were visible while Undo, Discard and Serve ended at y=381 below the viewport; Exit and Start were 32–38px; picker, shift and result copy repeated rules | Preserved the exact bakery engine but changed the phone layout to a fixed safe-area header/play/status shell; kept the customer/order visible, put all recipe choices in a 44px horizontal workbench, revealed Undo/Discard only after preparation and Serve only when ready, shortened copy, and compacted picker/result states | All nine required landscape sizes plus 390×844 portrait; picker, active preparation, wrong step, failure, replay, completion, rotation, exit and production shell | Wrong input, exact sequence, customer timeout failure, Replay, three-customer completion and guarded Exit pass; Level 1 paid exactly +80 once, unlocked Level 2, persisted 1 clear/3 served and resumed the exact active recipe after rotation; build/performance pass and 479/479 tests pass | `phase2-evidence/wave5-bakery/` full picker/shift before matrices, including `bakery-shift-568x320-before.png` | `phase2-evidence/wave5-bakery/` full shift after matrix, including `bakery-shift-568x320-after.png`, `bakery-success-568x320-after.png` and `bakery-390x844-after.png` | `734ae95` |
| Corner Café | At 568×320 the HUD ended at y=315 but its nine ingredient/station choices continued to y=436, actions to y=436 and feedback to y=473; the customer order and preparation tray rows duplicated the same three people; Undo, Discard and Serve stayed visible while inactive; Exit/picker controls measured 34–36px | Kept the protected three-tray engine, added live dish/patience summaries to each tray, hid the duplicated order row only on short phones, used a fixed safe-area workbench/status shell, revealed Undo/Discard/Serve contextually, hid unavailable Next on failure, and shortened instructions and feedback | All nine required landscape sizes plus 390×844 portrait; picker, live trays, wrong step, appliance wait/ready, failure, replay, completion, rotation, exit and production shell | Wrong input preserved the expected step; Hot Water and Toaster working/ready flows pass; Ben's natural timeout paid +0; all three customers completed for exact first-clear +59; replay paid +0; rotation retained Cup→Tea progress; guarded exit returned to the exact Old Market point with progression and wallet intact; build/performance pass and 482/482 tests pass | `phase2-evidence/wave5-cafe/` full picker/shift before matrices, including `cafe-shift-568x320-before.png` | `phase2-evidence/wave5-cafe/` full after matrix, including `cafe-shift-568x320-after.png`, `cafe-success-replay-568x320-after.png` and `cafe-390x844-after.png` | `7176c04` |
| Morning Mug Coffee | At 568×320 its four drink controls ended at y=335, actions at y=376 and feedback at y=413 below the y=315 HUD; orders duplicated three drink trays; inactive actions remained visible; Save & Exit was 34px | Preserved the resumable checkpoint contract, added live drink/patience tray summaries, hid duplicated orders on short phones, fitted a 52px workbench and 44px status, made actions contextual, and shortened copy without adding exit confirmation | All nine required landscape sizes plus 390×844 portrait; resume, wrong step, grinder/espresso waits, failure, completion, replay and production shell | Save & Exit restored Coffee Beans as the exact next step; natural failure paid +0; three espressos paid exact +50 once; replay paid +0; rotation retained the resumed drink; build/performance pass and 485/485 tests pass | `phase2-evidence/wave5-morning-mug/` full picker/shift before matrices | `phase2-evidence/wave5-morning-mug/` full after matrix, including `morning-mug-shift-568x320-after.png`, success, replay and portrait evidence | `1ac76f4` |

## Change register

| Change ID | System | UX reason | Files changed | Behaviour changed? | Save impact | Tests added | Status |
| --------- | ------ | --------- | ------------- | ------------------ | ----------- | ----------- | ------ |
| KW-P2-000 | Baseline protection | Prevent Phase 2 work from changing validated behaviour invisibly | Phase 1 report; this report | No | None | Existing 450-test suite rerun | Verified |
| KW-P2-001 | Global responsive shell | Prevent broken portrait gameplay and protect exact running state during rotation | `index.html`; `src/main.js`; `src/style.css`; `src/ui/ResponsiveShellController.js`; responsive shell tests; this report | Yes: non-River portrait now pauses; River remains portrait-supported | No schema or durable-state change; current state is persisted before sleep and resumed without offline advancement | 4 responsive shell tests, including all required landscape sizes and exact pause/wake calls | Verified |
| KW-P2-002 | Shared UI interaction foundation | Make every fixed and dynamically rendered button communicate input and availability consistently | `src/main.js`; `src/style.css`; `src/ui/InteractionFeedbackController.js`; interaction feedback tests; this report | No gameplay rule change; presentation state follows existing button state | None | 2 focused state/token tests; full 456-test suite | Verified |
| KW-P2-003 | Shared loading and error states | Explain scene imports and recover visibly when a lazy activity cannot open | `src/main.js`; `src/scenes/lazyScenes.js`; `src/style.css`; `src/ui/SharedOverlayController.js`; shared overlay tests; this report | No gameplay rule change; scene loading now has visible transient feedback | None | 2 focused copy/wiring tests; full 458-test suite | Verified |
| KW-P2-004 | Town HUD and navigation | Make the map dominant, remove equally prominent secondary choices, prevent mobile overlap and keep every destination reachable | `index.html`; `src/main.js`; `src/style.css`; `src/ui/TownMenuController.js`; `src/ui/OnboardingController.js`; `src/ui/SaveStatusController.js`; town-menu tests; this report | No gameplay rule change; the town menu adds a runtime-only pause reason while open | None; no schema, balance, reward, ownership, placement or progression data changed | 2 focused town-menu tests; full 460-test suite | Verified |
| KW-P2-005 | Lawn Care mobile gameplay | Keep the mower board and all essential feedback operable on the smallest landscape phone without changing 750-level parity | `index.html`; `src/scenes/LawnCareScene.js`; `src/style.css`; Lawn mobile UX tests; this report | No gameplay rule change; layout, labels and transient feedback only | None; active-session payload, campaign progress, rewards and town lawn state unchanged | 2 focused mobile UX tests plus existing 9 Lawn Care rule tests; full 462-test suite | Verified |
| KW-P2-006 | River Clear-Out portrait gameplay | Match the tall Tetris board to the user-approved portrait exception while preventing clipped controls and empty full-height panels | `index.html`; `src/scenes/RiverClearoutScene.js`; `src/style.css`; `src/ui/ResponsiveShellController.js`; responsive-shell and River mobile UX tests; this report | Yes: River now pauses in landscape and plays only in portrait; no level, completion, reward or difficulty rule changed | None; active attempt, 750-level campaign, wallet and save schema remain unchanged | 2 new River mobile UX tests, expanded orientation tests and full 464-test suite | Verified |
| KW-P2-007 | Waste Collection mobile gameplay | Keep the matching board dominant while making the tray, actions, feedback and one-time first job fully operable on short landscape phones | `index.html`; `src/scenes/WasteCollectionScene.js`; `src/style.css`; Waste mobile UX tests; this report | No gameplay rule change; only responsive layout, control visibility, labels and transient feedback changed | None; six-item town occurrence, 750 boards, exposed-card rules, five-slot failure, rewards, progress and save schema remain unchanged | 2 focused mobile UX tests plus 18 existing town/campaign tests; full 466-test suite | Verified |
| KW-P2-008 | Playground Power Wash mobile gameplay | Keep every washable cell and the wand visible while making all protected tool modes operable on short landscape phones | `index.html`; `src/scenes/PlaygroundPowerwashScene.js`; `src/style.css`; Power Washing mobile UX tests; this report | No gameplay rule change; canvas alignment, responsive layout, control visibility, labels and transient feedback only | None; active-session payload, 750 levels, first-clear records, rewards, town restoration and save schema remain unchanged | 2 focused mobile UX tests plus 8 existing service/parity tests; full 468-test suite | Verified |
| KW-P2-009 | Beach Cleanup mobile gameplay | Keep every sand tile and essential movement control operable while removing scroll-clipped controls and persistent optional choices | `index.html`; `src/scenes/BeachCleanupScene.js`; `src/style.css`; Beach mobile UX tests; this report | No gameplay rule change; responsive layout, progressive disclosure, contextual visibility, labels and transient feedback only | None; active-session payload, 750 levels, town occurrence, finds, bonuses, first-clear rewards and save schema remain unchanged | 2 focused mobile UX tests plus 8 existing Beach engine/service tests; full 470-test suite | Verified |
| KW-P2-010 | House Rescue mobile gameplay and cottage entry | Keep sorting, vacuuming and feedback operable on every landscape phone while ensuring a saved rescue is shown only at its owning cottage | `index.html`; `src/scenes/HouseRescueScene.js`; `src/scenes/HouseInteriorScene.js`; `src/scenes/TownScene.js`; `src/style.css`; House Rescue mobile UX tests; this report | No scoring, completion, level or reward rule change; layout, copy and feedback changed; an invalid cross-cottage scene entry is now blocked | None; active-session shape, cottage ownership, dirty/clean state, campaign, rewards and schema remain unchanged | 3 focused mobile/integration tests plus 15 existing House Rescue rule/save tests; full 473-test suite | Verified |
| KW-P2-011 | Fishing and Magnet Fishing mobile gameplay | Keep the river and aim point visible, remove inactive actions and prevent accidental loss of a pending cast without changing protected catch or reward rules | `index.html`; `src/scenes/FishingScene.js`; `src/style.css`; Fishing mobile UX tests; this report | No catch, timing, daily-limit, table, pity, reward or completion rule changed; layout, copy, contextual visibility and in-progress exit confirmation changed | None; active session, five-cast counters, inventory, aquarium, river cleanup, ledger, rewards and schema remain unchanged | 3 focused mobile/contract tests plus 14 existing Fishing service tests; full 476-test suite | Verified |
| KW-P2-012 | Little Bakery mobile gameplay | Keep the customer, order and every recipe control visible on short landscape phones while reducing repeated instructions and inactive actions | `index.html`; `src/scenes/BakeryScene.js`; `src/style.css`; Bakery mobile UX tests; this report | No recipe, timing, no-miss, level, score, reward or completion rule changed; layout, copy, contextual controls and in-progress exit confirmation changed | None; active session, 150-level progress, customer results, coins, ledger and schema remain unchanged | 3 focused mobile/contract tests plus 12 existing Bakery service tests; full 479-test suite | Verified |
| KW-P2-013 | Corner Café mobile gameplay | Keep the three-customer queue, live patience and every preparation action usable on short landscape phones without duplicated UI | `index.html`; `src/scenes/CafeScene.js`; `src/style.css`; Café mobile UX tests; this report | No recipe, arrival, patience, timing, no-miss, level, score, reward or completion rule changed; layout, live summaries, copy and contextual visibility changed | None; active session, three trays, 150-level progress, customer results, coins, ledger and schema remain unchanged | 3 focused mobile/contract tests plus 13 existing Café service tests; full 482-test suite | Verified |
| KW-P2-014 | Morning Mug mobile gameplay | Keep specialist drink preparation visible while protecting its unique resumable Save & Exit flow | `index.html`; `src/scenes/MorningMugScene.js`; `src/style.css`; Morning Mug mobile UX tests; this report | No recipe, arrival, patience, timing, level, reward, resume or completion rule changed; layout, copy and contextual visibility changed | None; persisted active checkpoint, trays, 150-level progress, coins, ledger and schema remain unchanged | 3 focused mobile/contract tests plus 12 existing Morning Mug tests; full 485-test suite | Verified |

### KW-P2-001 protected rule record

- Original rule: individual mini-games displayed separate portrait barriers at narrow widths; Town had no barrier and the shared Phaser loop and world systems continued running.
- Demonstrated problem: 390×844 rendered the complete town with a tall, overlapping HUD and active controls. Rotation could allow world time, NPCs and timed gameplay to continue while the player could not safely see or operate it.
- Smallest effective change: a shared controller detects the active scene and viewport, freezes the Phaser loop, adds an `orientation` pause reason to the world, NPC and municipal systems, and displays one global overlay. It wakes only after landscape returns. `RiverClearoutScene` is explicitly exempt.
- New rule: all active game scenes require landscape except River Clear-Out. Portrait pauses without completing, restarting, rewarding or mutating a level. Returning to landscape wakes the same scene and refreshes scale.
- Save compatibility: the save schema and payload are unchanged. The orientation reason is runtime-only; the current validated state is persisted before the game loop sleeps.
- Runtime proof: at 390×844 the body reported `orientationBlocked=true`, `rotate-device`, and the fixed sentence. After returning to 844×390 it reported `orientationBlocked=false`, the same `TownScene`, same location and same coin balance. Touch movement and wallet open/close then worked normally.

### KW-P2-002 verification note

The first live implementation attempt observed every CSS class mutation while adding its own class. Computer-controlled testing caught the resulting browser feedback loop before commit. The controller was corrected to observe only disabled and accessibility selection attributes, then resynchronise the clicked control and its sibling group once after the real click. The rebuilt game reported 206 controls, 206 enhanced controls and zero controls without a state at every required viewport. Wallet and Inventory tabs changed between `selected` and `normal` correctly, disabled controls remained disabled, the panel closed normally, portrait protection still worked and the browser runtime log contained no warnings or errors.

### KW-P2-004 verification note

- Original presentation: the saved Town state exposed 17 visible buttons at every measured size. Shop, Inventory, Resident, Animals, Welcome, Impact, Stories and Save competed directly with exploration. At 568×320 the first-job card and contextual action overlapped; at 736–844px the expanded checklist still touched the action; Waste Collection displayed the Town objective behind its own HUD.
- Smallest effective change: preserve all eight destination buttons and their existing controllers, but move them behind one `Menu` control. The menu contributes its own modal pause reason, closes in the capture phase before a destination opens, traps keyboard focus, closes with Escape and uses three columns only below 420px landscape height. The phone objective shows one short goal and its next action; larger tablets retain the full checklist.
- Runtime proof: the 568×320 menu displayed all eight destinations, including Save, with 44px controls and no internal scroll. A complete Tab cycle returned from Save to Close. Menu → Inventory kept the inventory dialog open, closing restored movement, and a real right-arrow input changed `WILLOW COMMONS · 1244, 1294` to `1257, 1294`.
- Integration proof: Waste Collection was entered through `Collect 4 pieces of rubbish`; only its own HUD was visible. Safe exit returned to `TownScene` at `1244, 1294` with `🪙 100`, proving no cleanup reward or location change was introduced.
- Responsive proof: every required landscape viewport reported exact viewport/document dimensions, no objective/action overlap and a 44px Menu target. A 390×844 rotation retained the exact Town position when returning to 844×390. Browser warnings/errors remained empty.

### KW-P2-005 verification note

- Original rule: the mower slides until a hedge, Level 1 has an 11-move limit and 9-move par, at least 50% is required, Undo retains five moves, town-job results update the exact lawn, and the original reward/progression/save rules apply across all 750 levels.
- Demonstrated problem: at 568×320 the direction rail began at y=312 in a 320px viewport and the status began at y=351. Players could see only the board and side panel, not operate the mower or read feedback. The 7×7 Level 1 board was also stretched to 378×206.
- Smallest effective change: retain the existing DOM, service and scene logic; make the short-landscape HUD a fixed header/play/status grid; move directions and Undo into unused width beside an aspect-correct board; hide the already-explained mower/legend copy at phone heights; keep Hint and Restart contextual; raise every active control to 44px; shorten only transient copy.
- Live input proof: a real horizontal swipe moved the mower from 7%/0 moves to 29%/1; the visible Undo restored 7%/0. Repeated live Hint plus direction clicks completed the certified path at 100%, 9/11 moves and 3 stars, raising the test wallet from 100 to 200 exactly once.
- Failure/recovery proof: alternating right/left inputs exhausted all 11 moves at 29%, displayed `This lawn needs another pass`, paid +0 and left 200 coins. Replay reset to 7%/0. Exit required the explicit second click and returned to `WILLOW COMMONS · 1257, 1226` without changing the wallet.
- Responsive/save proof: all nine landscape sizes kept the HUD and status inside the viewport, board aspect ratio at 1.0, document dimensions exact and smallest live button at 44px. A 390×844 rotation and a full reload both preserved 7%, 0/11 moves and 200 coins. Runtime warnings/errors remained empty.

### KW-P2-006 protected rule record

- Original rule: River Clear-Out was exempt from the shared landscape barrier, so its 10×16 Tetris board could run in portrait or landscape. Levels require at least 50% recovery; first clears award the existing reward once; failed attempts award nothing; the existing 750-level data, active-session payload and town integration are protected.
- Demonstrated problem: River is the user-approved portrait-only exception, but landscape remained playable. The old 390×844 layout left the board at 252×403, placed it beside a tall rail and left 252 pixels unused below the HUD. At 320×568 the production Hint button extended to x=373, outside the 320px viewport. The success card occupied a fourth implicit grid row and left most of the screen blank.
- Smallest effective change: make the orientation policy scene-specific, showing `Turn your device upright to play.` only for River; keep the same DOM and engine; place the board above a compact control rail; hide only secondary Par and Difficulty figures at 320px; give level and reward content centred cards; shorten transient copy and enforce 44px touch targets.
- New rule: River Clear-Out pauses safely whenever width exceeds height and resumes the exact attempt in portrait. Every other active game remains landscape-only. No completion, reward, queue, board, level or save rule changed.
- Live gameplay proof: left, rotate, right and Drop produced 1/34 pieces and enabled Undo; Undo returned exactly to 0/34. Six straight drops ended at 0%, displayed the failure result and paid +0. Replay restored 0/34; the certified solution completed at 100%, five pieces, three rows and three stars, raising the wallet from 210 to 310 exactly once.
- Responsive proof: every required landscape viewport reported `orientationBlocked=true`, `orientationExpected=portrait`, exact document dimensions and unchanged 0/34 state. Portrait phone/tablet checks retained a 0.625 board ratio, HUD and status inside the viewport, no document overflow and no visible control below 44px. The production 390×844 board measured 355×568 with no QA control; the 320×568 board measured 191×306 and all controls ended at or before x=307.
- Save/integration proof: the 310-coin wallet persisted after a full production reload. Return to Alder Brook restored `TownScene`; a separate active attempt kept 1/34 through the first Exit tap, required `Confirm Exit`, then returned without a reward. The save schema and durable payload are unchanged.

### KW-P2-007 verification note

- Original rules: the first Willow Commons occurrence contains exactly six authored pieces and awards its exact +100 once; the 750-level campaign permits only exposed cards, clears matching triples automatically, fails when five unmatched cards fill the tray, persists an active campaign, and pays each level's first-clear reward once.
- Demonstrated problem: at 568×320 the first-job Exit was 32px, all six item choices were 37px and Finish was 34px outside the 258px HUD. The campaign HUD ended at y=317 but its tray ended at y=384, actions at y=424 and status at y=465. The same controls remained below the viewport through 844×390. Campaign cards measured 24–43px across the required matrix.
- Smallest effective change: preserve the exact scene and service; use a fixed three-row campaign shell; keep the board in the main column and place the five-slot tray plus contextual actions in a 144px rail; maintain 44px card hit areas; keep only Hint visible initially, reveal Restart after a move, and show Restart alone on failure. The first job removes each collected choice and reveals Finish only after all six are collected.
- Live first-job proof: on an isolated fresh save, all six choices and Exit measured 44px, the HUD ended at y=283, Finish was absent initially, one pickup reduced the visible choices to five and reported `5 pieces left.`, and the sixth removed the final choice and revealed a 44px Finish at y=133–177. Completion paid the exact +100 and produced a 200 balance.
- Live campaign proof: Hint identified an exposed Broken toy; selecting it changed 30/0 to 29/1 and revealed Restart; Restart restored 30/0. Five deliberately different exposed cards filled the tray at 25 remaining, hid Hint and displayed `Tray full. Try another order.` Restart then reset the attempt. The certified Level 2 route cleared 30 cards in 10 matches, paid +100 and raised the main test wallet from 410 to 510. Replaying Level 2 displayed +0 and left 510 unchanged.
- Responsive proof: all nine landscape viewports reported exact document dimensions, board, tray, actions and status within the HUD, and zero visible controls below 44px. At 568×320 the production board occupied x=12–406 and y=107–278; the tray/action rail occupied x=412–556; status occupied y=284–314; the QA control was absent. A 390×844 rotation paused at 29 remaining and 1/5 tray, then resumed those exact values at 568×320.
- Save/integration proof: active campaign Exit changed to `Confirm Exit` while retaining 29/1, returned to `TownScene` only on the second tap and awarded nothing. Reload restored the 510 wallet and the normal `Open Waste Collection Campaign` town action. The save schema and durable payload are unchanged.

### KW-P2-008 protected rule record

- Original rules: Playground Power Wash has 750 levels; standard, precision and wide water nozzles plus soap; one continuous interpolated wash path; master dirt masks with progressive pass resistance; a 97% clean threshold that clears completion residue to 100%; protected embedded/standalone identity; first-clear-only campaign rewards; town-job restoration; and the existing level-specific reward cap.
- Demonstrated problem: the canvas was missing the class used by its responsive CSS. At 568×320 its 960×480 interactive surface extended to y=575 inside a frame ending at y=488; tools ended at y=579 and status at y=608. Controls remained below the viewport through 844×390. Tablet frames also clipped the intrinsic canvas, making pointer-to-dirt alignment unreliable. No wand was visible and the same Water/Soap values appeared in both the heading and side rail.
- Smallest effective change: add the intended canvas class, position the canvas exactly inside a 2:1 frame, add a non-interactive labelled wand, use a fixed three-row shell on short landscape screens, place the four protected tools in a two-by-two 148px rail, hide only duplicated supply meters at phone heights, reveal Restart after the first stroke, enforce 44px targets and shorten transient text.
- Live input proof: Precision was selected by default. Standard, Soap, Precision and Wide each changed the active service state and accepted touch input. A touch reduced water to 98% and revealed Restart; Soap reduced its own resource to 98%; Restart restored 0% clean, 100% water, 100% soap and the default Precision nozzle. Forty-five Wide sprays exhausted water to 1%, displayed recovery feedback, produced no result and paid +0.
- Completion/reward proof: the Commons town job completed at 100% and paid its exact Level 1 +101, raising the wallet from 510 to 611. Campaign Level 2 paid +100 once, raising it to 711; replay paid +0. Protected automated tests still prove raw 97% completes cleanly to 100%, residue is removed, Level 750 pays its exact capped +170 only once, and embedded identity/town restoration remain correct.
- Responsive proof: all nine landscape viewports reported exact document dimensions, a 2.0 board-frame ratio, canvas bounds aligned to the frame and zero visible controls below 44px. At 568×320 the production frame occupied x=34–381 and y=105–278, the tool rail x=408–556 and status y=284–314. The QA control and unused Restart were absent. The labelled wand remained visible without intercepting input.
- Rotation/save/integration proof: a 390×844 rotation paused an active Wide-nozzle attempt at the exact 0% clean, 97% water and 100% soap state; returning to 568×320 restored it exactly. Exit required `Confirm Exit`, retained the attempt after the first tap, returned to Town after the second and paid nothing. Reload restored the 711 wallet and normal campaign picker. The save schema and durable payload are unchanged.

### KW-P2-009 protected rule record

- Original rules: Beach Cleanup has 750 deterministic levels; walking rakes the tile being left; hidden rubbish is collected on its authored cell; Undo is bounded; No Undo, Light Foot and Clean Sweep remain optional bonus rules; town work restores South Shore; campaign first clears pay once; replay pays zero; Level 750 retains its capped +170 reward; and active attempts persist with their exact return point.
- Demonstrated problem: every required landscape size overflowed. At 568×320 the board occupied y=92–470, the five walking controls y=473–504 and status y=508–533 inside a HUD clipped at y=317. At 1280×720 the board still ended at y=774, controls at y=819 and status at y=857. Exit was 32px, optional challenge buttons 30px, primary actions 36px and walking controls 31–39px. Optional bonuses remained as three persistent choices before the player needed them.
- Smallest effective change: keep the exact scene and service, but use a fixed three-row landscape shell; place the board in the main column and a three-column D-pad in the side rail; hide the already-duplicated found-items card only on short screens; put optional rules behind one `Bonus` disclosure; reveal Undo and Restart only after a move; enforce 44px controls; and shorten only labels and transient feedback.
- Live gameplay proof: a real production swipe changed the active attempt from 0 to 1 move and revealed Restart. In the development build, Hint-driven visible controls completed the town route in 59 real steps, raked 100% and found 1/1. A blocked left step displayed `An obstacle blocks that step.`; Undo displayed `Step undone.` and removed itself; Restart restored 0 moves, 0/24 raked and hid both contextual controls. No terminal failure state exists in the protected Beach engine, so no artificial fail rule was introduced.
- Bonus/reward proof: the compact Bonus panel exposed No Undo, Light Foot and Clean Sweep together; No Undo toggled on and off with explicit feedback. The South Shore town result paid its protected +6, raising the development wallet from 26,010 to 26,016. Campaign Level 2 paid +100 once, raising it to 26,116; replay paid +0. Existing tests still prove every representative certified route, the Level 750 +170 cap, exact town restoration and rollback rules.
- Responsive proof: all nine required landscapes reported exact document dimensions, board, rail and status within the HUD, no visible control outside the viewport and zero visible controls below 44px. At 568×320 the board frame occupied x=11–404 and y=103–279, the rail x=409–557 and status y=284–314. Level 750 displayed the complete 15×13 board and all 50 hidden items inside the same frame. The production build contained neither QA nor premature Restart controls.
- Rotation/save/integration proof: a 390×844 rotation paused campaign Level 2 at the exact two-move state and resumed it unchanged at 568×320. A full reload restored the same level and moves. Exit retained an active Level 3 after the first tap, required `Confirm Exit`, returned to South Shore after the second and paid nothing. Reload kept the 26,116 wallet and normal campaign action. Production entry through the real South Shore town target and physical touch-swipe input both passed with no browser warnings or errors.

### KW-P2-010 protected rule record

- Original rules: House Rescue retains 750 deterministic levels, three rubbish categories, wave-bound sorting, +2 for a correct sort, −1 for a mistake, a 95% vacuum threshold, progressive stain strength, equipped-vacuum effects, dirty-home scheduling, first-clear campaign progress, the original reward formula, homeowner gifts, exact return points and resumable active attempts.
- Demonstrated problem: the baseline 568×320 floor and bins ended at y=406.5 while the status ended at y=481.5; at 667×375 and 812×375 the same play surface remained clipped; at 736×414 the board only just fit but feedback did not. The right-aligned desktop HUD also left most of the viewport unused. Exit/QA measured below 44px, instructions repeated the same rules and the result offered `Find next home` and `Return to Willowmere` even though both called the same function.
- Smallest effective change: preserve the service, state and generated content; make the HUD a fixed safe-area three-row shell; let sorting and vacuum floors consume the remaining height; keep three finger-sized bins in a compact rail; move development QA to the header; shorten only visible copy; add contextual selection and result feedback; and merge the duplicate return controls. A development-only `house` query selects a cottage spawn for repeatable integration QA and is excluded from production.
- Live input and completion proof: selecting Eggshells reported `Eggshells selected. Choose a bin.`; Recycling produced score −1/mistakes 1/remaining 9 and `Wrong bin. −1. Try another.`; Organic then produced score 1/remaining 8 and `Correct! +2.` All remaining items were sorted through visible buttons. Real vacuum swipes removed 120 of 180 layers, then targeted swipes completed at the protected 97% threshold. The result showed score 17, 90% accuracy and exactly +96, changing the wallet from 26,116 to 26,212 once.
- Cottage-integration proof: the live replay exposed a pre-existing mismatch in which a Shoreward Cottage save could be displayed after entering Morningbell Cottage. The durable completion correctly cleaned Shoreward, proving the save itself was not corrupted. The entry guard now shows `A rescue is already in progress`, names `Morningbell Cottage`, disables `Rescue saved elsewhere` at Millstone Cottage and never transitions. At the owning cottage the button remains enabled as `Resume House Rescue`. No save shape or reward logic changed.
- Responsive proof: all nine required landscapes reported exact document dimensions, HUD/floor/bins/status inside the viewport, zero clipped visible items and zero visible controls below 44px. At 568×320 the floor occupied x=12–423 and y=137.5–278; bins occupied x=428–556 in three 45.5px targets; status occupied y=283–313. Picker and result fit at 568×320; the result has one 280×44 return action.
- Rotation/save/production proof: portrait at 390×844 displays the full centred rotation icon and only `Turn your device sideways to play.` while hiding House Rescue. Returning to 844×390 preserved Level 2 sorting with 8 items, score 2 and 0 mistakes exactly. The production build and performance budget pass, a 568×320 production smoke test has exact document dimensions, no development QA control and no browser warnings or errors. All 473 automated tests pass, including deterministic Level 750 data, exact reward endpoints, persistence rollback and active sorting/vacuum reload.

### KW-P2-011 protected rule record

- Original rules: Commons Pond, Reedbank and South Harbour share five daily fishing casts; Magnet Fishing has a separate five-cast limit; casts outside water consume nothing; a used cast remains used after safe cancellation; catches, aquarium placement and releases persist exactly once; the eight magnet finds, rare/treasure pity counters, visible river-item removal, 180-game-minute respawn delay and all coin values remain protected. The Sealed Coin Tin pays exactly +350.
- Demonstrated problem: at 568×320 the baseline HUD occupied x=6–562 and y=90.5–314, covering nearly all playable water. Cast and disabled Reel/Retrieve were both permanently visible at 40px while Exit measured 35.5px. The same empty-result sentence was displayed before a catch existed. At 667×375 the card still spanned 655px, and wider layouts retained a 329–342px bottom-left card instead of using the right-side space.
- Smallest effective change: preserve the scene, targeting, timers and service; move the HUD to a safe-area side rail that becomes 172–220px around the 812px transition; enforce 44px controls; show Cast only while idle/missed/successful and Reel/Pull only during the bite/ready phase; hide the action row while waiting; omit the result row until a real result exists; shorten visible prompts; and require a second Exit tap only while a cast is pending.
- Live Fishing proof: the first timed attempt was deliberately allowed to expire and displayed `The fish escaped. Cast again.` with Cast restored. The next real Cast/Reel completed inside the bite window, displayed `River Minnows added to your inventory.`, changed total/streak from 0/0 to 1/1 and left the coin balance unchanged. Re-entering and catching again remained functional, proving the result state returns cleanly to the next Cast action.
- Live Magnet proof: the first Cast changed casts from 5 to 4. The first Exit tap changed only the control to `Confirm Exit`; the second returned to Town with four casts, zero finds and the same 110-coin wallet, proving no pending reward. A subsequent visible Cast/Pull produced `Sealed Coin Tin · +350 coins!`, recorded the named result once and changed the wallet from 110 to 460. Repeated isolated verification produced the same exact +350 delta without duplication inside a pull.
- Responsive and rotation proof: both modes were captured before and after at 568×320, 667×375, 736×414, 812×375, 844×390, 1024×768, 1180×820, 1280×720 and 1366×768. Every after size had exact document dimensions, zero overflow and no visible control below 44px. At 568×320 the after rail occupied x=390–562 and y=76.4–314 after a result, leaving the full left 390px and upper water visible; the baseline card covered x=6–562. Portrait at 390×844 hid the HUD behind `Turn your device sideways to play.` and retained the exact `success` phase, remaining cast count and named result after returning to landscape.
- Save/build proof: Fishing service tests still prove exact once-only inventory insertion, aquarium/release handling, cancellation, pity, river cleanup, +350 ledger reconciliation, rollback and daily reset. The production build and performance budget pass; the built 568×320 Town shell loads with a 568×319.5 canvas and zero overflow. All 476 automated tests pass. No state schema, level data, balance formula, reward table or town integration changed.

### KW-P2-012 protected rule record

- Original rules: Little Bakery retains 150 shifts, exact ordered recipe steps, three preparation trays where configured, named customers, the no-miss requirement, patience and duration limits, per-level deterministic menus, first-clear-only rewards, unlock progression, replay score improvement, persistence rollback and legacy progress import. Level 1 pays exactly +80 on its first clear.
- Demonstrated problem: at 568×320 the baseline HUD covered nearly the whole customer/preparation scene. Its scroll container ended at y=315, but the seven-step list continued through y=335 and the Undo, Discard and Serve row ended at y=381. Only four recipe choices were visible without scrolling. Exit measured 32px and Start 38px. Disabled controls were permanently displayed, and picker, active-shift and result copy repeated rules already communicated by the highlighted step.
- Smallest effective change: preserve the Bakery scene and service; use a fixed safe-area phone shell; keep the customer, compact order and patience visible above the preparation area; place every ingredient and appliance in a horizontal 44px workbench; reveal Undo/Discard only after a valid step and Serve only after the product is ready; keep a fixed 44px status row; and shorten only transient, picker and result copy.
- Live input and failure proof: choosing Mixed Seeds while Bread Dough was highlighted left the expected step unchanged and displayed `That order needs Bread dough next.` Choosing Bread Dough advanced to Kneading Bench and revealed only the 44px Undo and Discard actions. Allowing Mia's patience to expire produced `Shift needs another try`, 0% accuracy, +0 coins and no unlock; Replay returned to a fresh preparation state.
- Completion and reward proof: the visible workbench was used to complete every highlighted step and Serve action for all three customers. The result displayed `Shift complete!`, three stars and `+80`; the wallet changed exactly from 1,160 to 1,240, Level 1 recorded one clear and three served, and Level 2 unlocked. Existing tests also prove replay pays no first-clear coins twice and failed persistence restores the prepared final order safely.
- Responsive and rotation proof: active Bakery play was captured before and after at 568×320, 667×375, 736×414, 812×375, 844×390, 1024×768, 1180×820, 1280×720 and 1366×768. Every after size had exact document dimensions, zero overflow and no visible control below 44px. At 568×320 the header ended at y=62, order/patience at y=132, contextual actions at y=204, full workbench at y=266 and status at y=316. Portrait at 390×844 hid play behind the single sentence `Turn your device sideways to play.`; returning to 568×320 retained Kneading Bench as the exact expected step with no progress or reward change.
- Exit/build proof: the first active-shift Exit tap changed to `Confirm Exit`, retained the prepared step and wallet, and the second returned to the exact Old Market position with the 1,240 balance and Bakery progression intact. The production build and performance budget pass; its 568×320 shell has zero overflow. All 479 automated tests pass. No save shape, level data, recipe, score, balance, reward or town integration rule changed.

### KW-P2-013 protected rule record

- Original rules: Corner Café retains 150 deterministic shifts, three preparation trays, up to three active customers, authored arrival gaps, exact ordered recipe steps, appliance wait/ready interactions, per-customer patience, no misses, first-clear-only rewards, replay best scores, persistence rollback and legacy progress import. Level 1 pays exactly +59 on its first clear.
- Demonstrated problem: at 568×320 the original HUD ended at y=315, but the workbench continued through y=436, its action row ended at y=436 and status ended at y=473. Only the first four of nine preparation choices were visible. The three customers were repeated in both an order row and a tray row, while disabled Undo, Discard and Serve remained visible. Exit measured 34px and the picker controls 36px.
- Smallest effective change: preserve the Café service and physical three-tray model; add dish and live patience to each tray so it can replace the duplicate order row only on short landscape phones; keep all nine choices in a 52px horizontal workbench with the expected step sorted first; reveal Undo/Discard only after progress and Serve only when the current dish is ready; hide the unavailable Next action on failure; and shorten only picker, transient and result copy.
- Live input, appliance and failure proof: Bread while Cup was expected left the recipe at Cup and displayed `That dish needs Cup next.` Cup advanced to Tea Bag and revealed 44px Undo/Discard actions. Hot Water changed from `working…` to `ready. Tap it again.` before advancing, and the later Toaster used the same protected two-tap interaction. Allowing Ben's patience to expire naturally produced `Shift needs another try`, retained one served customer, paid +0 and unlocked nothing. Replay reset the transient shift.
- Completion and reward proof: every visible recipe control was used to prepare Tea, Tea with Milk and Buttered Toast for Mia, Ben and Ella. The first result recorded three served customers, unlocked Level 2 and paid exactly +59, changing the wallet from 1,240 to 1,299. A complete second run displayed `Best score saved. Replay pays no coins.`, +0 and left the wallet at 1,299. The failure view removes unavailable Next while preserving Replay and Return.
- Responsive and rotation proof: active play was captured before and after at 568×320, 667×375, 736×414, 812×375, 844×390, 1024×768, 1180×820, 1280×720 and 1366×768. Every after size had exact document dimensions, zero overflow and no visible control below 44px. At 568×320 the trays ended at y=114, contextual order/actions at y=162, workbench at y=268 and status at y=316. The complete customer/preparation room remains visible around those compact overlays. A 390×844 rotation hid the shift behind `Turn your device sideways to play.` and returned to the exact Cup-complete/Tea-next state with zero customers served.
- Save/exit/build proof: live tray patience changed from 100% to 97% without another input. The first Exit tap retained Tea as the expected step and changed only to `Confirm Exit`; the second returned to `OLD MARKET · 305, 1142`. The 1,299 wallet, one cleared shift and Level 2 unlock remained intact. The production build and performance budget pass; its 568×320 shell has zero overflow. All 482 automated tests pass. No save shape, recipe, arrival, patience, score, reward, balance or town integration rule changed.

### KW-P2-014 protected rule record

- Original rules: Morning Mug is a separate 150-shift campaign with three drink trays, exact ingredient/appliance order, no misses, first-clear-only rewards and an immediate persisted checkpoint. Save & Exit and page shutdown must restore the exact unfinished drink; Level 1 pays +50 once.
- Demonstrated problem and change: at 568×320 the workbench, actions and status ended at y=335, y=376 and y=413 below the y=315 HUD. Orders duplicated trays and controls were 34–36px. The protected engine was retained while trays gained live drink/patience summaries, duplicate phone orders were hidden, all live controls became at least 44px, inactive actions became contextual and copy was shortened. Save & Exit deliberately remains one tap because it safely suspends rather than abandons the shift.
- Live proof: Coffee Beans chosen before Small Cup was rejected without advancing. After Small Cup, Save & Exit returned to `HIGH STREET · 3460, 805`; re-entry restored Coffee Beans next, zero served and the same wallet. Coffee Grinder and Espresso Machine each passed working/ready/tap-again. A natural customer timeout paid +0. Three completed Small Espressos unlocked Level 2 and paid exactly +50 (1,299→1,349); a full replay paid +0.
- Responsive/build proof: all nine required landscapes had exact dimensions, zero overflow and no visible control below 44px; 390×844 showed the single rotate sentence and returned to the same resumed drink. The production 568×320 shell loaded without overflow. Build and performance budget pass; all 485 tests pass. No save, checkpoint, recipe, timing, progression, reward or town integration rule changed.

## Baseline interaction inventory

### Town — persistent controls

Essential and always visible:

- Town identity/location
- Current objective
- Coins
- Contextual interaction
- Movement controls on touch layouts
- One menu/pause entry

Secondary and suitable for one organised menu:

- Shop
- Inventory
- Resident profile
- Animal Friends
- Impact
- Stories
- Save diagnostics
- Welcome/login history
- Optional commerce status

Contextual:

- Job finder
- Object placement controls
- Nearby resident, animal, building, bin or job action
- Zoom when the map is being explored

The baseline town exposed 17 visible buttons at every measured viewport. KW-P2-004 reduces the same saved Town state to 10 visible controls: four essential/contextual actions plus six movement/zoom controls. All eight secondary destinations remain available in the Town menu. The smallest-phone objective now presents one goal and one next action instead of repeating the complete three-job list.

## Deferred ideas

These are not implementation requirements until supported by evidence or the missing v4 reference:

- New decorative icon artwork or house-derived ornamentation
- Art-direction changes beyond established v3 tokens
- Rewriting validated mini-game mechanics for novelty
- Repricing, reward rebalancing or level-curve changes
- Removing legacy import/reconciliation code
- Adding a broad music system that does not exist in the validated baseline

## Verification rules

Every implementation entry must include:

1. Before and after screenshots.
2. Small phone, modern phone, tablet, 1280×720 and portrait checks at minimum.
3. Browser console review.
4. Relevant focused tests.
5. Full automated regression suite before commit.
6. Save/reload and scene-return checks where state is involved.
7. A commit hash recorded in both registers.
